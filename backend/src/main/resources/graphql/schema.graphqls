type Query {
    books: [Book]
    bookById(id: ID): Book
}

type Book {
    id: ID!
    title: String!
    isbn10: String
    pages: Int
    publisher: String!
    author: String!
    subject: String!
    publishedAt: String!
}