type Query {
    books: [BookDTO]
    bookById(id: ID): BookDTO
}

type BookDTO {
    id: ID!
    title: String!
    isbn10: String
    pages: Int
    publisher: String!
    author: String!
    subject: String!
    publishedAt: String!
}